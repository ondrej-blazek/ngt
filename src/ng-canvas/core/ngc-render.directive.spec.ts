import { NgcRenderDirective } from './ngc-render.directive';

describe('NgcRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgcRenderDirective();
    expect(directive).toBeTruthy();
  });
});
