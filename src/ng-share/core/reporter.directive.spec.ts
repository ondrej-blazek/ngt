import { ReporterDirective } from './reporter.directive';

describe('ReporterDirective', () => {
  it('should create an instance', () => {
    const directive = new ReporterDirective();
    expect(directive).toBeTruthy();
  });
});
