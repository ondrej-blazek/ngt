import { ChronosDirective } from './chronos.directive';

describe('ChronosDirective', () => {
  it('should create an instance', () => {
    const directive = new ChronosDirective();
    expect(directive).toBeTruthy();
  });
});
