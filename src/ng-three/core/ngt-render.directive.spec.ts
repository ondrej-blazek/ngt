import { NgtRenderDirective } from './ngt-render.directive';

describe('NgtRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgtRenderDirective();
    expect(directive).toBeTruthy();
  });
});
