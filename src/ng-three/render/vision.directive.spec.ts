import { VisionDirective } from './vision.directive';

describe('VisionDirective', () => {
  it('should create an instance', () => {
    const directive = new VisionDirective();
    expect(directive).toBeTruthy();
  });
});
