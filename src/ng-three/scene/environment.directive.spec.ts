import { EnvironmentDirective } from './environment.directive';

describe('EnvironmentDirective', () => {
  it('should create an instance', () => {
    const directive = new EnvironmentDirective();
    expect(directive).toBeTruthy();
  });
});
