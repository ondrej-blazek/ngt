import { GeometryDirective } from './geometry.directive';

describe('GeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new GeometryDirective();
    expect(directive).toBeTruthy();
  });
});
