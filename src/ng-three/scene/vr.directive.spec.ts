import { VrDirective } from './vr.directive';

describe('VrDirective', () => {
  it('should create an instance', () => {
    const directive = new VrDirective();
    expect(directive).toBeTruthy();
  });
});
