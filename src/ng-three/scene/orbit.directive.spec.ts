import { OrbitDirective } from './orbit.directive';

describe('OrbitDirective', () => {
  it('should create an instance', () => {
    const directive = new OrbitDirective();
    expect(directive).toBeTruthy();
  });
});
