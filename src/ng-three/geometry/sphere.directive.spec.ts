import { SphereDirective } from './sphere.directive';

describe('SphereDirective', () => {
  it('should create an instance', () => {
    const directive = new SphereDirective();
    expect(directive).toBeTruthy();
  });
});
