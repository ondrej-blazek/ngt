import { GltfMeshDirective } from './gltf.mesh.directive';

describe('GltfMeshDirective', () => {
  it('should create an instance', () => {
    const directive = new GltfMeshDirective();
    expect(directive).toBeTruthy();
  });
});
