import { GltfSceneDirective } from './gltf.directive';

describe('GltfSceneDirective', () => {
  it('should create an instance', () => {
    const directive = new GltfSceneDirective();
    expect(directive).toBeTruthy();
  });
});
