import { OrthoDirective } from './ortho.directive';

describe('OrthoDirective', () => {
  it('should create an instance', () => {
    const directive = new OrthoDirective();
    expect(directive).toBeTruthy();
  });
});
