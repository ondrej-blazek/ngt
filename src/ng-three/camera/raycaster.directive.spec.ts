import { RaycasterDirective } from './raycaster.directive';

describe('RaycasterDirective', () => {
  it('should create an instance', () => {
    const directive = new RaycasterDirective();
    expect(directive).toBeTruthy();
  });
});
