import { ProjectorDirective } from './projector.directive';

describe('ProjectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectorDirective();
    expect(directive).toBeTruthy();
  });
});
