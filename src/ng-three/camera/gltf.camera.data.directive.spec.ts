import { GltfCameraDataDirective } from './gltf.camera.data.directive';

describe('GltfCameraDataDirective', () => {
  it('should create an instance', () => {
    const directive = new GltfCameraDataDirective();
    expect(directive).toBeTruthy();
  });
});
