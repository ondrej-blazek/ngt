import { DomeDirective } from './dome.directive';

describe('DomeDirective', () => {
  it('should create an instance', () => {
    const directive = new DomeDirective();
    expect(directive).toBeTruthy();
  });
});
