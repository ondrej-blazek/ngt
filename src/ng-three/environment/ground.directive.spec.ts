import { GroundDirective } from './ground.directive';

describe('GroundDirective', () => {
  it('should create an instance', () => {
    const directive = new GroundDirective();
    expect(directive).toBeTruthy();
  });
});
