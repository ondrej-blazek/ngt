import { CubePanoramaDirective } from './cube-panorama.directive';

describe('CubePanoramaDirective', () => {
  it('should create an instance', () => {
    const directive = new CubePanoramaDirective();
    expect(directive).toBeTruthy();
  });
});
