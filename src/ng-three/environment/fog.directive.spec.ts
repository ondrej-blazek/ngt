import { FogDirective } from './fog.directive';

describe('FogDirective', () => {
  it('should create an instance', () => {
    const directive = new FogDirective();
    expect(directive).toBeTruthy();
  });
});
