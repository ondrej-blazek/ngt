import { HemisphereLightDirective } from './hemilight.directive';

describe('HemisphereLightDirective', () => {
  it('should create an instance', () => {
    const directive = new HemisphereLightDirective();
    expect(directive).toBeTruthy();
  });
});
