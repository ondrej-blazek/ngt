import { GltfLightDirective } from './gltf.light.directive';

describe('GltfLightDirective', () => {
  it('should create an instance', () => {
    const directive = new GltfLightDirective();
    expect(directive).toBeTruthy();
  });
});
