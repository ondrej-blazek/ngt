import { NgdCloseEventDirective } from './ngd-close-event.directive';

describe('NgdCloseEventDirective', () => {
  it('should create an instance', () => {
    const directive = new NgdCloseEventDirective();
    expect(directive).toBeTruthy();
  });
});
