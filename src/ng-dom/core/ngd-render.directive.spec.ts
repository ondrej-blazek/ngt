import { NgdRenderDirective } from './ngd-render.directive';

describe('NgdRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgdRenderDirective();
    expect(directive).toBeTruthy();
  });
});
